package event

const (
	ProgressStart  = "progress:start"
	ProgressUpdate = "progress:update"
	ProgressFinish = "progress:finish"
	ProgressRetry  = "progress:retry"
	ProgressError  = "progress:error"

	Finish = "finish"
)
